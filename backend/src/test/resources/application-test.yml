spring:
  sql:
    init:
      mode: never

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

logging:
  level:
    root: WARN
    com.rndlab: INFO
    org.springframework.security: WARN
