<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rndlab.backend.mapper.ProcessMapper">

    <resultMap id="ProcessMap" type="com.rndlab.backend.entity.Process">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="processType" column="process_type"/>
        <result property="temperature" column="temperature"/>
        <result property="pressure" column="pressure"/>
        <result property="duration" column="duration"/>
        <result property="description" column="description"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="ProcessMap">
        SELECT * FROM processes ORDER BY name
    </select>

    <select id="findById" resultMap="ProcessMap">
        SELECT * FROM processes WHERE id = #{id}
    </select>

</mapper>

