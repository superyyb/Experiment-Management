<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rndlab.backend.mapper.UserMapper">

    <resultMap id="UserMap" type="com.rndlab.backend.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="fullName" column="full_name"/>
        <result property="teamId" column="team_id"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        
        <collection property="roles" ofType="com.rndlab.backend.entity.Role" column="id" 
                    select="selectRolesByUserId" fetchType="lazy"/>
    </resultMap>
    
    <resultMap id="RoleMap" type="com.rndlab.backend.entity.Role">
        <id property="id" column="id"/>
        <result property="roleName" column="role_name"/>
        <result property="description" column="description"/>
    </resultMap>
    
    <select id="selectRolesByUserId" resultMap="RoleMap">
        SELECT r.id, r.role_name, r.description
        FROM roles r
        INNER JOIN user_roles ur ON r.id = ur.role_id
        WHERE ur.user_id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserMap">
        SELECT 
            u.id,
            u.username,
            u.password,
            u.email,
            u.full_name,
            u.team_id,
            u.status,
            u.created_at,
            u.updated_at
        FROM users u
        WHERE u.username = #{username}
    </select>

    <select id="findById" resultMap="UserMap">
        SELECT 
            u.id,
            u.username,
            u.password,
            u.email,
            u.full_name,
            u.team_id,
            u.status,
            u.created_at,
            u.updated_at
        FROM users u
        WHERE u.id = #{id}
    </select>

</mapper>

